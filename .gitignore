node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel/

# Firebase CLI
.firebase/
firebase-debug.log
firestore-debug.log

# firebase-applet-config.json is a TEMPLATE (no secrets).
# Real credentials live in frontend/.env (already gitignored above).
